#include <stdio.h>
#include <math.h>
#include <iostream>

using namespace std;

int main(){

    printf("\n sqrt(25) = %.2f", sqrt(25));
    printf("\n sqrt(64) = %.2f", sqrt(64));
    printf("\n sqrt(81) = %.2f", sqrt(81));

    cin.get();
    return 0;
}